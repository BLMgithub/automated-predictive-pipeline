config {
  type: "view",
  database: "automated-predictive-pipeline",
  schema: "source_dataset",
  name: "raw_order_items",
  tags: ["build_pipeline"]
}

SELECT
  order_id,
  product_id,
  created_at,
  shipped_at,
  sale_price,
  status
FROM `${database()}.core_ecom_dataset.order_items`